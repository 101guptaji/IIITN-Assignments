#include<iostream>
/*
The #include directive tells the compiler to include a file and 
#include<iostream> tells the compiler to include the standard iostream file which contains declarations of all the standard input/output library functions.
*/
using namespace std;
/*namespace std is used to import the entirety of the std namespace into the current namespace of the program. It pulls all type definitions into the current scope.
*/
int main()
{
    cout<<"Hello World";
    return 0;
}